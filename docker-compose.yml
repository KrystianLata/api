version: '3.7'
services:
  web:
    build: .
    command: uvicorn app.main:app --host 0.0.0.0 --port 5000 --reload
    volumes:
      - .:/workspace
    working_dir: /workspace/
    # stdin_open: true
    # tty: true
    ports:
      - "5000:5000"

